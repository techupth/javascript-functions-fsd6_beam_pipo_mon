// Start coding here
let calculator = {
  add: function (a, b) {
    return a + b;
  },
  subtract: function (a, b) {
    return a - b;
  },
  multiply: (a, b) => a * b,
  divide: (a, b) => a / b,
};

let addResult = calculator.add(10, 20);
console.log(addResult);

let divedeResult = calculator.divide(3000, 10);
console.log(divedeResult);
